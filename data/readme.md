# Data

This folder contains two text files used during the training process. Please update the file paths inside these files to match your local environment.

The train and test files were generated using `fst1_short` in the notebook `extension/train_our_model.ipynb`.

Instructions for downloading the dataset can be found in the notebook `extension/load_data.ipynb`.
